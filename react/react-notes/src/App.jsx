import React from 'react'

const App = () => {
  return (
<>

{/* // components
// instead of one huge html mess
// you break UI into small chunks
// navbar, product card, footer, etc

// jsx
// lets you write html inside js
// so UI and logic live together

// props
// how a component gets told what to show
// like passing info into a function

// state (useState)
// normal variables are dead to react
// state is what actually updates the screen
// click -> setState -> react redraws */}


{/* hooks */}

{/* // useEffect
// do something when something changes
// page loads, api runs, timer starts

// useContext
// one shared place for data
// user, theme, auth
// no passing props through the whole tree

// useRef
// store something without causing a re-render
// or poke the real DOM when needed

// useMemo / useCallback
// stop react from redoing heavy work
// remember results and functions */}


{/* basic react */}

{/* // server components
// run on the server
// grab data
// send ready HTML

// client components ("use client")
// run in browser
// buttons, forms, state, effects
// anything interactive */}

</>
);
};

export default App